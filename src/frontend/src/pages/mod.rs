pub mod page_builder;
pub mod page_writer;

//use super::components;
//use crate::PageProps;
//use anyhow::Result;

/*
pub struct Page {
    pub html: components::Html,
}

impl components::Component for Page {
    fn write(&self, props: &PageProps, buf: &mut String) -> Result<()> {
        self.html.write(props, buf)?;

        Ok(())
    }
}
*/
